{
  "name": "ai-ecommerce-search-system",
  "version": "1.0.0",
  "description": "Professional monorepo structure for AI Enhanced E-Commerce Search",
  "scripts": {
    "start": "docker-compose up --build",
    "stop": "docker-compose down",
    "logs": "docker-compose logs -f",
    "seed": "docker-compose exec backend npm run seed"
  }
}
